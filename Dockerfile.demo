FROM python:3.11-slim

WORKDIR /app

COPY monitor_backend.py index.html config.demo.json ./

RUN cp config.demo.json config.json

ENV CONFIG_PATH=/app/config.json
ENV HOST=0.0.0.0
ENV PORT=8000

EXPOSE 8000

CMD ["python", "-u", "monitor_backend.py"]
