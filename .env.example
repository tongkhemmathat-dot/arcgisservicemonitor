# Generate a key with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Then copy this file to .env and paste the key below.
# Keep .env secret — never commit it.

ENCRYPTION_KEY=
